
*run: v2.exe
 ./$<

all.files : : echo '$glob' > $@

ml.files : all.files
  egrep '^[^#]+.ml$' $< > $@

ocaml.modules : ml.files
  cat $< | sed 's|\(.*\).ml$|\1|' > $@

include ~/code/jenga/rulelib/ocaml.jenga
